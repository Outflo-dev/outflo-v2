/* ==========================================================
   OUTFLO — SIGN IN PAGE
   File: src/app/(onboarding)/sign-in/page.tsx
   Scope: Enter the Sign In access composition
   Last Updated:
   - date: 2026-08-09
   - note: establish Sign In as the existing-account access surface
   ========================================================== */

import {
    SignInComposition,
} from "@/compositions/onboarding";

export default function SignInPage() {
    return <SignInComposition />;
}
